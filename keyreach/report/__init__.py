"""Report rendering and the machine-readable report contract.

``schema.py`` and the generated ``report.schema.json`` landed in roadmap R0.3.
``render.py`` and ``templates/`` landed the terminal, JSON and Markdown
renderers in R0.8; the HTML renderer joined them in R2.9
(``implementation_plan.md`` §9).
"""

from __future__ import annotations
