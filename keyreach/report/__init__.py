"""Report rendering and the machine-readable report contract.

Landed so far: ``schema.py`` and the generated ``report.schema.json``
(roadmap R0.3). The terminal, Markdown and HTML renderers arrive with
``render.py`` and ``templates/`` in R0.8 (``implementation_plan.md`` §9).
"""

from __future__ import annotations
