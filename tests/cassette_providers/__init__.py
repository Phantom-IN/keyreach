"""A fixture provider package driven entirely by a cassette (roadmap R0.6).

Exercises the real `Provider` contract against the real `ProbeContext` with no
network involved, which is exactly how every shipped provider will be tested
from R1.1 onward.
"""

from __future__ import annotations
