"""keyreach test suite.

A package (rather than a bare directory) so fixture packages such as
``tests.dummy_providers`` can be imported absolutely. Relative imports are
banned repository-wide by ruff's ``flake8-tidy-imports`` configuration.
"""
